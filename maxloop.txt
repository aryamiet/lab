#include<stdio.h>
int main()
{
int n,i;
float num,max;
printf("enter the number of elements");
scanf("%d",&n);
max=-99999999;
for(i=0;i<n;++i);
{
printf("enter number");
scanf("%f",&num);
if(num>max)
{
maxium=num;
}
}printf("maximum number is %.2f\n",maximum);
return 0;
}